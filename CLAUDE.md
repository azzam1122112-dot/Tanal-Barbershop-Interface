# CLAUDE.md

دليل معماري موجّه للمطوّرين وأدوات الذكاء للعمل على هذا المستودع. README.md يغطّي التشغيل والنشر؛ هذا الملف يغطّي القرارات والأنماط.

## ما هو المشروع

نظام تشغيل وولاء لصالون حلاقة رجالي (TANAL). Next.js 15 (App Router) + React 19 + Prisma 6 + PostgreSQL، واجهة عربية RTL بالكامل. ثلاثة فضاءات:

- **واجهة الحلاق** `/barber` — موبايل أولًا، تسجيل الزيارات والبحث عن العملاء وفتح/إغلاق جلسة الصندوق.
- **لوحة المدير** `/dashboard` — العملاء، الزيارات، التقارير، الحملات، الولاء، جلسات الصندوق، واتساب، الإعدادات.
- **APIs** `/api/*` — معالجات رقيقة فوق طبقة الخدمة.

## أوامر أساسية

```bash
npm run dev          # تطوير
npm run typecheck    # tsc --noEmit
npm test             # vitest run (يحتاج PostgreSQL يعمل + seed)
npm run lint
npm run build
npm run prisma:migrate
npm run prisma:seed
```

> اختبارات عدة (cash-session, visit-service, daily-close, customer-service...) تستخدم `PrismaClient` حقيقيًا وتحتاج قاعدة بيانات تعمل وحساب مدير من الـ seed. الباقي اختبارات نقية.

## طبقات الكود

```
app/api/*      معالجات HTTP رقيقة: صلاحية → تحقق Zod → استدعاء الخدمة
app/(barber|dashboard)/*  صفحات React
components/*    واجهات (barber/ و dashboard/)
lib/*          منطق الأعمال الحقيقي (مصدر الحقيقة)
prisma/*       schema + migrations + seed
```

**القاعدة:** لا تضع منطق أعمال في معالجات الـ API أو المكوّنات. ضعه في `lib/*` لأنه قابل للاختبار وإعادة الاستخدام بين الواجهات.

## أنماط لا تكسرها

### هيكل اللوحة وقراءة الجلسة (أداء)
- **هيكل اللوحة تخطيط لا مكوّن صفحة.** الشريط الجانبي وشريط الجوال ومبدّل الفروع وحالة الاشتراك كلها في `app/dashboard/(shell)/layout.tsx`. التخطيط المشترك لا يُعاد بناؤه عند التنقّل بين مسارات المجموعة، فالجلسة وقائمة الفروع وحالة الاشتراك تُقرأ مرة واحدة لا مرة في كل صفحة. صفحة الدخول خارج المجموعة عمدًا لأنها بلا جلسة. `DashboardShell` في `components/dashboard/ui.tsx` صار ترويسة صفحة فقط — **لا تُعِد إليه أي استعلام أو قراءة جلسة**.
- **`getRequestSession` محفوظة بـ `cache` من React** (`lib/auth/http.ts`). الجلسة تُقرأ عدة مرات في الطلب الواحد (التخطيط + الصفحة + بوابات الصلاحية) وكل قراءة `findUnique` بأربعة `include`. النطاق طلب واحد — لا تسرّب بين الطلبات.
- **`lastUsedAt` لا يُحدَّث في كل طلب** بل بفاصل `SESSION_LAST_USED_REFRESH_MS`. الحقل مؤشر صيانة لا يُقرأ في أي مسار تشغيلي، وتحديثه في كل طلب كان كتابة في DB مع كل صفحة ونداء API.
- **لا `backdrop-filter` على أصناف متكررة.** `.dashboard-panel` يتكرر ~56 مرة في صفحة واحدة؛ كل نسخة طبقة تركيب تُعاد ضبابيتها في كل إطار تمرير. ولا `background-attachment: fixed` على `body` — يُبطل الخلفية عند كل بكسل تمرير.
- **`loading.tsx` يرسم منطقة المحتوى فقط**، فيبقى التنقّل ظاهرًا وقابلًا للنقر أثناء انتقال الصفحة.

### تطبيق الحلاق المثبّت (PWA)
- واجهة الحلاق تُثبَّت كتطبيق مستقل ببيان خاص `public/barber.webmanifest` بنطاق `/barber`، مربوط من `app/barber/layout.tsx`. البيان الجذري `manifest.webmanifest` يبقى للموقع كاملًا ويبدأ من `/` — **لا تجعل `start_url` الجذري يشير إلى `/barber`** وإلا فتح تثبيتُ الموقع شاشةَ الحلاق.
- **عامل الخدمة `public/barber-sw.js` يخزّن الهيكل لا البيانات.** المخزَّن حصرًا: أصول `/_next/static` المبصَّمة، الأيقونات، وصفحة `/barber-offline.html`.
  - **`/api/*` شبكة فقط بلا استثناء** — الجهاز قد يكون مشتركًا بين حلاقين، ومبلغ أو رصيد نقاط من نسخة قديمة خطأ تشغيلي لا تحسين أداء.
  - تنقّل الصفحات: شبكة أولًا، وعند الفشل **صفحة «لا اتصال»** لا صفحة محفوظة فيها بيانات عميل.
  - عند تغيير المخزَّن ارفع `CACHE_VERSION` — التفعيل يحذف النسخ الأقدم.
- `BarberPwa` في `components/barber/pwa.tsx` يجمع: التسجيل، وزر التحديث عند توفّر نسخة، ودعوة التثبيت، ومؤشر انقطاع الشبكة. أشرطة سفلية بأولوية واحدة (انقطاع ← تحديث ← تثبيت). التسجيل في الإنتاج فقط — عامل الخدمة يتعارض مع إعادة التحميل الساخن في التطوير.
- **لا تُضِف دعوة تثبيت ثانية في الصفحات** (كانت في `barber/login` فأُزيلت): نافذة تعترض شاشة الدخول تؤخّر الحلاق عن عمله.
- **اختصار التطبيق** `‎/barber?action=search` يفتح شاشة العمل مع تركيز حقل الجوال (لوحة الأرقام جاهزة). يُقرأ الاستعلام من `window` لا عبر `useSearchParams` حتى لا يدخل حدّ Suspense في شاشة يجب أن تُرسم فورًا.
- **البيان لا يُسبق تخزينه** في عامل الخدمة: اسمه غير مُبصَّم، فتخزينه يعني ألا يصل أي تعديل عليه للأجهزة المثبَّتة إلا برفع `CACHE_VERSION`. وهو يُقرأ وقت التثبيت/التحديث فقط — أي مع شبكة أصلًا.

> **بناء بلا إيقاف خادم التطوير:** `next dev` و`next build` يتشاركان `.next`، وتشغيلهما معًا يُفسد المجلد (`Cannot find module for page: /_document`). استخدم `NEXT_DIST_DIR=.next-build npm run build`.

### نظام التصميم (globals.css مصدر الحقيقة)
- **الأسطح:** كل سطح أبيض يمر عبر `.dashboard-panel` أو `.barber-card` — لا تكتب `rounded-3xl border border-salon-line bg-white shadow-*` يدويًا. كانت البطاقة مكتوبة بأربع صيغ مختلفة في ٨ ملفات فاختلفت الزوايا والظلال بين شاشة وأخرى.
- **سلّم أنصاف الأقطار:** `rounded-xl` (0.75rem) للعناصر والشرائح، و`rounded-2xl` (1rem) للبطاقات والنوافذ. لا تُدخل مقاسًا ثالثًا.
- **الظلال طبقتان** لكل مستوى (`--shadow-sm/md/lg`): ظل قريب يرسم الحافة وآخر بعيد يرسم العمق. الظل الواحد العريض يبدو رخيصًا.
- **سياسة الأوزان:** `font-black` (900) **للأرقام والمبالغ وحدها**. العناوين 700، التسميات والنص 600. الثِقَل يعني «هذا هو الرقم» لا أنه الوزن الافتراضي. استخدم `.lux-number` للمبالغ و`.lux-title`/`.lux-eyebrow`/`.lux-section-title` للعناوين.
- **الذهب بخِسّة:** `.lux-edge` (خط ذهبي علوي) للأسطح البطولية فقط — ترويسة الصفحة. الذهب على كل لوح يُفقده معناه.
- **شارات الحالة:** `Badge` من `components/dashboard/ui.tsx` بنقطة لونية — اللون وحده لا يفرّق «مؤكدة» عن «ملغاة» لمن لا يميّز الأحمر عن الأخضر. لا تكتب شارة محلية جديدة.

### التجاوب ومناطق الأمان
- `viewport-fit=cover` مفعّل، فكل عنصر لاصق أو ثابت عند حافة الشاشة يحتاج `env(safe-area-inset-*)`: ترويسة الحلاق، شريط جوال اللوحة، درج التنقّل، أشرطة الإجراءات السفلية، والأوراق المنزلقة (bottom sheets).
- **لا `maximumScale` ولا `userScalable: false`** — منع التكبير يقطع الوصول عن ضعاف البصر، والشاشات فيها مبالغ وأرقام جوال تُقرأ ولا تُخمَّن.
- واجهة الحلاق: عمود واحد على الجوال، وعمودان من `md` عبر `.barber-grid` (العمل والبحث يمينًا، الصندوق والملخص يسارًا). شاشة تسجيل الزيارة تبقى عمودًا واحدًا لأنها تسلسل خطوات.
- **جداول اللوحة:** `TablePanel` يلفّها بـ `.table-scroll-wrap` + `.table-scroll` (تلاشٍ عند الحافة يدل على وجود تكملة). سجل الزيارات — أكثر شاشة استخدامًا — يبدّل إلى **بطاقات تحت `lg`** بدل السحب الأفقي، والتفاصيل مشتركة بين العرضين في `VisitDetails` فلا يتفرّع السلوك.

### تسجيل الدخول (بلا معرّف مؤسسة)
- **لا يُطلب من أي مستخدم كتابة معرّف مؤسسة.** الإدارة: بريد + كلمة مرور. الحلاق: جوال + PIN. المنصّة: بريد + كلمة مرور.
- المؤسسة تُحلّ بهذا الترتيب: النطاق الفرعي إن وُجد (`getKnownLoginOrgSlug` من المضيف فقط) ← وإلا من الهوية وبيانات الاعتماد معًا عبر `resolveLoginIdentity`.
- `resolveLoginIdentity` تتحقق من بيانات الاعتماد ضدّ كل هوية مطابقة عبر المؤسسات: مطابقة واحدة = دخول، أكثر من واحدة = عرض **أسماء الصالونات** للاختيار، صفر = رسالة عامة.
- **قاعدة أمنية:** لا تُعرض قائمة المؤسسات إلا **بعد** نجاح كلمة المرور/الرمز — وإلا لأمكن معرفة أين يعمل شخص بمجرد إدخال بريده. وعدد عمليات التحقق محدود بخمسة مرشّحين لمنع استنزاف الخادم.

### الصلاحيات
- بوابات الوصول في `lib/auth/access.ts`. المعالجات تستخدم `requireBarberApi` / `requireDashboardApi` / `requireAdminApi` من `lib/auth/http.ts`.
- الجلسات مخزّنة في DB كـ SHA-256 hash للتوكن، والكوكي `httpOnly` + `sameSite=lax` + `secure` في الإنتاج.
- `middleware.ts` يتحقق فقط من وجود الكوكي (حارس مبدئي) + يفحص Origin على الطلبات المغيّرة للحالة (CSRF). التحقق الفعلي من الصلاحية يتم داخل المعالج.

### نطاق المشرف بالفروع (Supervisor salon scoping)
- **المالك/المدير على مستوى المؤسسة** (كل الفروع). **المشرف مقيّد بفروعه المسندة** عبر جدول الربط `StaffSalon` (User↔Salon، متعدد لمتعدد). الإسناد يُدار من شاشة «الموظفون» (مالك/مدير فقط).
- `session.salonId` هو **الفرع النشط للعرض فقط**: قيمة = فرع واحد، و`null` = عرض مجمّع (كل الفروع للمالك/المدير، وكل الفروع المسندة للمشرف). للمشرف لا يُقبل إلا فرع من فروعه أو `null`.
- `session.scopedSalonIds` هو **القيد الأمني الحقيقي**: `null` للمالك/المدير، أو مصفوفة فروع المشرف. مشرف بلا فروع مسندة = لا وصول (الجلسة تُرفض في `getAuthSession`).
- **القاعدة الجوهرية:** لا تكتب `...(salonId ? { salonId } : {})` يدويًا في أي استعلام — النمط يتسرّب: عند العرض المجمّع يصبح بلا قيد وهو صحيح للمالك وخطأ للمشرف. استخدم دائمًا مساعدات `lib/auth/salon-scope.ts`:
  - `salonScopeWhere(session)` → فلتر Prisma للفرع.
  - `effectiveSalonIds(session)` → مصفوفة للخدمات التي تستقبل `salonIds`.
  - `dashboardScope(session)` → `{ organizationId, orgWhere, salonWhere, salonIds, activeSalonId, isAggregate }` جاهز للصفحات.
  - `isSalonAllowed` / `assertSalonAllowed` للتحقق من معرّف قادم من العميل.
- **الطفرات التي تستقبل معرّفًا من العميل لازم تُقيَّد صراحةً بالفرع + المؤسسة:** `closeCashSession` و`closeBarberDay` (تمرّر `organizationId` + `salonIds`)، وتعديل/إلغاء الزيارة (`salonIds` في `AdminMeta`). هذا أيضًا يسدّ عزل المستأجرين.

### توزيع الصلاحيات (بوابات `lib/auth/access.ts`)

| البوابة | مالك | مدير | مشرف | يحكم |
|---|:--:|:--:|:--:|---|
| `canAccessDashboard` | ✅ | ✅ | ✅ | دخول اللوحة |
| `canOperateLoyalty` | ✅ | ✅ | ❌ | **تشغيل** الولاء: مكافآت المدير، الحملات، واتساب، العملاء. المشرف فرعي النطاق ولا يملك بيانات العملاء أو الحملات المؤسسية. |
| `canSetLoyaltyPolicy` | ✅ | ✅ | ❌ | **سياسة** الولاء: معدّل النقاط وقواعد المكافآت |
| `canTransferBarbers` | ✅ | ✅ | ✅ | عرض الحلاقين ونقلهم بين الفروع |
| `canManageBarbers` | ✅ | ✅ | ❌ | إنشاء/حذف حلاق وتغيير رمز دخوله |
| `canManageStaff` | ✅ | ✅ | ❌ | الموظفون، الخدمات، الإعدادات |
| `canPayCommissions` | ✅ | ✅ | ❌ | صرف العمولات وعكسها، ودفتر المستحق/المدفوع التراكمي |
| `canWithdrawBranchSafe` | ✅ | ✅ | ❌ | سحب نقد من خزنة الفرع (تسليم للمالك أو إيداع بنكي) |
| `canViewFinancials` | ✅ | ✅ | ❌ | البيان المالي الشهري ومقارنة الفروع — ربحية المؤسسة |
| `canManageOrganization` | ✅ | ❌ | ❌ | الفروع (إنشاء/تعديل/حذف) والباقة |

- **القاعدة الجامعة للنقد:** كل ما **يُخرج** نقدًا من الخزنة بيد واحدة يحتاج مالك/مدير — صرف عمولة أو سحب من الخزنة سواء. التحصيل من الحلاق يبقى للمشرف لأنه **يُدخل** النقد لا يُخرجه. كان صرف خمسين ريالًا محجوبًا عن المشرف بينما سحب رصيد الخزنة كاملًا متاحًا له.
- **دفتر العمولات تراكمي عبر كل الفروع** (وهذا لازم لصحة الصرف: تقسيمه بالفرع يجعل حلاقًا منقولًا يُصرف له مرتين). ولأنه يتجاوز حدود الفرع فهو محجوب عن المشرف — يرى مستحق فروعه عن الفترة لا دَين المؤسسة التراكمي.

- **لماذا سياسة الولاء مركزية:** `RewardRule` و`Campaign` ومعدّل النقاط كلها على مستوى المؤسسة حتى يجمع العميل نقاطه من أي فرع. لو مُنح المشرف تعديلها لغيّر مشرفُ فرعٍ قواعدَ المؤسسة كلها.
- **نقل الحلاق:** المشرف يرسل `salonId` فقط (أي حقل آخر → 403)، والفرعان المصدر والهدف يجب أن يكونا ضمن فروعه. النقل مرفوض (409) إذا كان للحلاق جلسة صندوق مفتوحة حتى لا يبقى كاش معلّق في الفرع القديم. يُسجَّل كـ `barber.transferred`.
- **الحذف مقابل التعطيل:** حذف الفرع/الموظف/الحلاق متاح فقط عند غياب أي سجل تشغيلي مرتبط؛ غير ذلك تُعاد 409 مع اقتراح التعطيل حفاظًا على التقارير وسجل التدقيق. كما يُمنع حذف فرعٍ هو الوحيد المسند لمشرف (وإلا فقد وصوله كليًا).

### معالجة الأخطاء
- أخطاء الأعمال المتوقعة (تحقق، صلاحية، حالة) تُرمى كـ `BusinessError` من `lib/errors.ts`.
- المعالجات تُعيد الأخطاء عبر `toErrorResponse` / `safeErrorMessage` من `lib/http/error-response.ts`:
  - `BusinessError` → تُعرض رسالته العربية بحالته.
  - أي خطأ آخر → يُسجَّل عبر `logger` ويُعاد رسالة عامة 500 (لا تسريب تفاصيل داخلية).
- **لا تُعِد `error.message` مباشرة للواجهة** لخطأ غير مصنّف.

### المال والتجميع
- كل تجميع لإجماليات زيارات يمر عبر `aggregateVisitTotals` في `lib/visits/visit-totals.ts` — مصدر حقيقة واحد لجلسة الصندوق والإغلاق اليومي. لا تكرّر منطق cash/card/net/points.
- المبالغ `Decimal` في DB؛ حوّلها بـ `Number(...)` عند الحساب و`roundMoney` عند الجمع.
- **الإعدادات تُقرأ دائمًا عبر `getEffectiveSettings`** من `lib/settings/system-settings.ts` (فرع ثم مؤسسة). `systemSettings.findFirst({})` بلا قيد قد يعيد إعدادات مستأجر آخر فتُحتسب النقاط بقيم خاطئة.

### الإيصالات
- `netAmount` هو المبلغ المدفوع فعليًا بعد الخصم، وكل التجميعات المالية وجلسة الصندوق مبنية عليه.
- `Visit.customerId` اختياري: عملية الزائر تحفظ الخدمة والتحصيل والعمولة بلا اسم أو جوال، ولا تنشئ حساب ولاء أو حركة نقاط. لا تستخدم سجل «عميل نقدي» مشتركًا.
- ربط العميل يتم قبل الدفع للاستفادة من رصيده، لكن كسب النقاط واستبدالها وتأكيد الزيارة يحدثان معًا بعد تأكيد التحصيل فقط.
- تُحتسب النقاط على المبلغ بعد الخصم أو قبله وفق إعداد الفرع.
- كل زيارة تحصل على `invoiceNumber` تسلسلي لكل فرع/سنة عبر `issueInvoiceNumber` **داخل معاملة الزيارة** (عدّاد `InvoiceCounter`)، فلا يتكرر الرقم ولا ينكسر التسلسل.
- مستند العميل الحالي **إيصال مبيعات غير ضريبي**؛ لا تسمّه فاتورة ضريبية ولا تضف رقم ضريبي أو رمز QR ضريبي ما دامت المنصة لا تطبّق ضريبة القيمة المضافة.
- ملف PDF يُنشأ في الخادم من `lib/invoicing/receipt-pdf.ts` بخط عربي مضمّن، ويُحمى مساره بنفس نطاق الإيصال: الحلاق يرى زياراته فقط، والإدارة ضمن فروعها الفعلية.
- زر المشاركة يجهّز ملف PDF ثم يستخدم مشاركة الملفات الأصلية في الجهاز متى كانت مدعومة، وإلا ينزّله كبديل واضح للمستخدم.

### العمولات
- تُحسب وقت الزيارة بـ `calculateVisitCommission` وتُخزَّن على `Visit.commissionAmount` وعلى كل سطر خدمة مع نسبته — فتغيير النسب لاحقًا **لا يغيّر المستحقات التاريخية**.
- **تعديل مبلغ الزيارة يعيد توزيع المستحق بالنسب المخزَّنة على السطور** (`recalculateVisitCommission`)، لا يتركه ولا يعيد حسابه بنسب اليوم. تركُه كان يجعل الحلاق مستحقًا عمولة على مال لم يُحصَّل ويُظهر «نسبة فعلية» تتجاوز 100%؛ وإعادةُ حسابه بالنسب الحالية تكسر قاعدة تجميد المستحق التاريخي. زيارة بلا سطور تُقاس بالتناسب على نسبتها الفعلية السابقة.
- الوعاء هو `netAmount` بعد الخصم: لا عمولة على خصم لم يدفعه العميل.
- أسبقية النسبة: `Service.commissionRate` ← `Barber.commissionRate` ← `SystemSettings.defaultCommissionRate`. الوعاء يُوزَّع على الخدمات بنسبة قيمة كل سطر.
- تقرير المستحقات (`getCommissionReport`) يقرأ المخزَّن فقط ويستثني الملغاة تلقائيًا.

### المصروفات النثرية
- `CashExpense` يُربط بجلسة الصندوق المفتوحة، فيصبح **الكاش المتوقع = عهدة البداية + مقبوضات الكاش − مصروفات الدرج**، وفرق الصندوق مفسَّرًا بدل أن يظهر كعجز.
- الحلاق يسجّل مصروف جلسته هو فقط وضمن جلسة مفتوحة. لا إضافة ولا حذف على جلسة مغلقة (409) حتى لا تتغيّر أرقام إغلاق محفوظة.
- **`SystemSettings.barberExpenseLimit` سقف ما يسجّله الحلاق بنفسه** (افتراضيًا 200، وصفر = بلا سقف). مصروف الدرج يُنقص «الكاش المتوقع»، فبلا سقف يغطّي الحلاق عجزًا وقت الإغلاق بمصروف كبير والتدقيق يكشفه بعد وقوعه. القيد على **المسجِّل** لا على البند: المدير يسجّل أي مبلغ باسمه.
- `expensesTotal` يُثبَّت على الجلسة عند الإغلاق كلقطة.
- مدير الفرع يسجّل من `/dashboard/expenses` داخل فروعه المسندة فقط، والمالك/مدير المؤسسة يريان كل الفروع. `expenseDate` هو تاريخ المصروف التشغيلي، أما `createdAt` فيبقى وقت الإدخال والتدقيق.
- `paymentSource=CASH_DRAWER` وحده يُخصم من الكاش المتوقع ويلزم جلسة مفتوحة. `EXTERNAL` لا يغيّر مطابقة الدرج، لكنه يدخل مع كل المصروفات في **المتبقي للمؤسسة = صافي المبيعات − عمولات الحلاقين − المصروفات**.
- إجماليات التقرير تُحسب بالـ aggregate على كامل الفترة؛ حدّ 500 يخص صفوف العرض فقط ولا يجوز أن يحدّ المجموع.

### الطبقة المالية الشهرية
- **`organizationContribution` في `lib/finance/contribution.ts` هي التعريف الوحيد للربح:** `صافي المبيعات − تكلفة المنتجات المباعة − عمولات الحلاقين المستحقة − المصروفات`. **لا تكتب المعادلة يدويًا في أي شاشة.** كانت مكتوبة بأربع صيغ (الرئيسية والتقارير تخصمان المصروفات بلا عمولات، والمصروفات ومقارنة الفروع تخصمهما معًا) فيقرأ المالك رقمين مختلفين لنفس الفترة حسب الشاشة.
- **تكلفة المنتجات من `VisitProduct.unitCost`** — لقطة وقت البيع كنسبة العمولة تمامًا. **لا تقرأ `Product.costPrice` وقت التقرير:** تعديل تكلفة منتج واحد يعيد كتابة مجمل ربح كل شهر مضى. السطر بلا تكلفة يُحتسب صفرًا ويُعدّ في `unpricedProductUnits` فيُعلَن نقص التغطية بدل أن يظهر الربح أعلى من حقيقته.
- **أساس الاستحقاق لا النقد.** العمولة تدخل قائمة الدخل في **شهر الزيارة** (`Visit.commissionAmount`) لا في شهر صرفها. لو حُسبت نقديًا لظهر شهرٌ صُرفت فيه دفعتان خاسرًا وشهرٌ لم يُصرف فيه شيء ممتازًا. **وصرف العمولة ليس مصروفًا** — خُصم الاستحقاق مرة واحدة وقت الزيارة، فتسجيله مصروفًا يخصمه مرتين؛ لذلك يُعرض في بيان نقدي منفصل.
- **مفتاح الشهر `YYYY-MM` بتقويم الرياض** عبر `toRiyadhMonthKey` / `parseRiyadhMonthKey` / `riyadhMonthKeysBetween`. زيارة عند 21:30 UTC من آخر الشهر تنتمي للشهر التالي بتوقيت الرياض، والتقسيم يتم داخل PostgreSQL بـ `to_char(("visitedAt" AT TIME ZONE 'UTC') AT TIME ZONE 'Asia/Riyadh', 'YYYY-MM')` فلا يعتمد على منطقة خادم Node.
- **التجميع في القاعدة لا في Node.** `getFinancialPeriodReport` يستخدم `$queryRaw` مجمَّعًا؛ **لا تبنِ التقرير الشهري على `getVisitsForReport`** — تلك تجلب كل صفوف الزيارات بخمسة `include` وهو مقبول ليوم واحد وكارثي لاثني عشر شهرًا × عدة فروع. شرط المدى يبقى على العمود الخام ليستفيد من فهارس `visitedAt`.
- **المصروفات تُجمَّع على `expenseDate` دائمًا لا على `createdAt`** — مصروف ٣٠ يوليو يُدخَل ٢ أغسطس ينتمي ليوليو.
- **عكس سند صرف يُحتسب في شهر العكس** لا في شهر السند الأصلي، وإلا أُعيدت كتابة تقرير شهر سبق أن قرأه المالك واعتمد عليه.
- **الأشهر الخالية تُعرض بصفر** لا تُحذف: قائمة الصفوف تُبنى من مفاتيح المدى لا من نتائج الاستعلام، وإلا قرأ المالك تسعة أشهر ظنًّا أنها اثنا عشر.
- حركة العمولة لكل حلاق (`getCommissionMovement`) تُنسب **بفرع الحلاق الحالي لا بفرع الزيارة**، تمامًا كـ `getCommissionLedger`، وإلا اختلف «رصيد آخر المدة» عن المتبقي في شاشة الصرف عند أول حلاق منقول بين فرعين.
- المدى محدود بـ `MAX_FINANCIAL_MONTHS`، و`/dashboard/finance` و`/dashboard/salons-compare` محكومتان بـ `canViewFinancials` (مالك/مدير مؤسسة) — كلتاهما تعرض ربحية المؤسسة. **إخفاء الصفحة من التنقّل ليس صلاحية:** الرابط المباشر يفتحها، فالبوابة في الصفحة نفسها.
- الشاشات التشغيلية تبقى يومية: اليومي أداة تشغيل والشهري أداة ملكية.
- **لا يوجد إقفال شهري بعد:** الأرقام تُحسب لحظة الفتح، فإلغاء زيارة قديمة يغيّر تقرير شهرها. جدول تجميد (`MonthlyFinancialClose`) هو الخطوة التالية إن لزم تجميد الأرقام.

### بوابة العميل والمواعيد
- `/my/[token]` صفحة عامة بلا جلسة: الرمز نفسه bearer secret قصير العمر، ولا يُخزن خامًا؛ تحفظ قاعدة البيانات `Customer.portalTokenHash` مع تاريخ الإصدار والانتهاء. إصدار رابط جديد يدوّر الرمز السابق، والصفحة `noindex` ولا تكشف إلا بيانات صاحبها وتُغلق لمؤسسة موقوفة.
- **البوابة أربعة تبويبات على تخطيط مشترك** (`app/my/[token]/layout.tsx`): بطاقتي · مواعيدي · العروض · زياراتي. كانت عشرة ألواح متتالية بالوزن نفسه بلا تنقّل — المكافآت مفتَّتة على ثلاثة، والحجز (الفعل الوحيد) خامسًا تحت ألواح ساكنة. التخطيط يحلّ الرمز مرة، و`generateMetadata` فيه لا في الصفحات فيرث كل تبويب `noindex` وبيان التثبيت.
- **محمّل بيانات لكل تبويب** في `lib/customers/portal-view.ts`، و`getPortalIdentity` محفوظة بـ `cache` من React كـ `getRequestSession`. **لا تُعِد دالة واحدة تجلب كل شيء:** `listBookableSalons` تدور على الفروع بثلاثة استعلامات لكل فرع، وكانت تُنفَّذ لمن يفتح «زياراتي». التقسيم أخرج حزمة الحجز من التبويبات الثلاثة الأخرى أيضًا (177 بايت للعروض مقابل 7 kB للمواعيد).
- **تأكيد الحجز لوحٌ يحلّ محلّ النموذج لا سطر أسفله.** السطر كان يظهر في اللحظة التي تنكمش فيها الشاشة: تفريغ الاختيار يُخفي المدى والملخّص، وإعادة تحميل الشبكة تطوي عشرات الخانات إلى «جاري جلب الأوقات…» — فتقفز الصفحة تحت عين الباحث عن تأكيده. اللوح يشغل المكان نفسه فلا يتحرك شيء.
- **`bookingArriveEarlyMinutes` إرشاد عرضٍ لا قيد** (افتراضيًا 10، صفر = لا يُعرض): لا يزحزح `startAt` ولا يدخل حساب التداخل. يُعرض في ثلاثة مواضع — إيصال التأكيد، وبطاقة الموعد القادم، وقائمة الحجوزات — ودائمًا **كوقت فعلي** («احضر 5:20») لا فرقًا مجرّدًا.
- **لا تذكير تلقائي قبل الموعد.** واتساب يدوي بالكامل بقرار معماري، ولا يوجد جدول اشتراكات push للعملاء (`BarberPushSubscription` للحلاقين وحدهم). إضافته لاحقًا تحتاج جدولًا جديدًا + مجدولًا + حقل `remindedAt` على `Appointment` حتى لا يُرسَل مرتين.
- **`Campaign` منمذج ولا يُعرض في البوابة.** العائق ليس إغفالًا: `evaluateCampaignEligibility` يطلب `grossAmount` لحساب خصم النسبة ولفحص السقف، ووقت البوابة لا زيارة ولا مبلغ. عرضها يلزمه **فصل الأهلية إلى شقّين** — شقّ لا يعتمد على المبلغ (فعّالة، داخل النافذة، الاستهداف مطابق، لم تُستنفد `maxUsesPerCustomer`) تستعمله البوابة، وشقّ يعتمد عليه تبقى الزيارة عليه. لا تختلق مبلغًا وهميًا. وانتبه أن `campaign.name` مكتوب للاستهلاك الداخلي («استرجاع المنقطعين») فعرضه حرفيًا يخبر العميل بتصنيفه.
- **سياسة عدم الحضور لا تُعرض لمن لا مخالفة عليه** — كانت تظهر لكل عميل: تحذير استباقي قبل أن يُخطئ أحد. صاحب المخالفة يراها، والباقي يجدها في إيصال حجزه.
- **التسجيل في الولاء بمسارين:** الحلاق (عند إنشاء العميل، ويسلّمه الرابط من ملف العميل)، أو العميل نفسه من `/join` العام.
- **قرار خصوصية في `/join`:** رقم مسجّل مسبقًا **لا يُعاد له الرابط أبدًا** — وإلا لأمكن لأي شخص إدخال أرقام ليعرف من هم عملاء الصالون ويطّلع على سجل زياراتهم. الرد محايد ويُحال لأخذ الرابط من الحلاق. النموذج محدود المعدّل بـ IP عبر `consumeRateLimit` قبل أي كتابة، ومحكوم بحد العملاء في الباقة وبحارس الاشتراك.
- المواعيد اختيارية ولا تمنع walk-in. `assertNoOverlap` يمنع حجزين متداخلين لنفس الحلاق. الموعد يُغلق كـ `COMPLETED` فقط عبر ربطه بزيارة (`completeAppointmentWithVisit`).

### مدة الموعد

- **مدة الموعد تُشتقّ من خدماته عبر `resolveAppointmentServices`** في `lib/appointments/appointment-duration.ts`. **لا تُقبل `durationMinutes` من العميل** — تُقرأ من الكتالوج كأسعار المنتجات في `resolveProductLines` تمامًا. رقمٌ يرسله العميل («30» مع تسعين دقيقة خدمات) يسرق جدول الحلاق ويوقع ثلاثة عملاء على كرسي واحد. التجاوز اليدوي متاح لمسار الموظف وحده.
- **`AppointmentService.durationMinutes` لقطة وقت الحجز** كنسبة العمولة و`VisitProduct.unitCost`: تعديل مدة خدمة في الكتالوج لا يزحزح موعدًا سبق أن أُبلغ به العميل. الجدول لا يكرّر `organizationId` — العزل محفّز يقارن الأبوين على نمط `VisitService`.
- **التقريب لأعلى لمضاعف الفترة في مسار العميل فقط.** `isBarberOnDuty` يشترط بداية على الشبكة، فخدمة 45 دقيقة على شبكة 30 تحجز 60: لا فتاتَ ربعِ ساعة لا يُحجز ولا يُرى، وهامشُ تنظيف مجانًا. **حجز الموظف لا يُقرَّب** — حرٌّ بالتصميم، وتقريبُه يحجب وقتًا لا يعمل فيه أحد (لذلك `slotMinutes` اختيارية).
- **الشبكة تُحسب بمدة الخدمات المختارة**: `getCustomerBookingSlots` يستدعي دالة الاشتقاق نفسها التي يستدعيها الحجز. شبكةٌ حُسبت بمدة تخالف المحجوزة تعرض وقتًا يُرفض عند الضغط. ولهذا ترتيب شاشة العميل: فرع ← **خدمات** ← حلاق ← وقت، وتُعاد الشبكة عند كل تغيير في الاختيار.
- **نافذة فحص التداخل موسّعة للخلف** بمقدار `MAX_APPOINTMENT_MINUTES` عبر `lib/appointments/overlap-window.ts`. المدد أعمدة لا تعبير SQL فالتقاطع يُفحص في Node، وجلبُ يوم رياض واحد كان يفترض أن كل موعد ينتهي داخل يومه: موعد 11:30 مساءً بمدة ساعتين لا يظهر عند فحص اليوم التالي فيُحجز فوقه. السقف والتوسيع ثابتٌ واحد عمدًا — لا يُرفع أحدهما دون الآخر فتعود الثغرة صامتة.
- **لا هامش تنظيف مستقل بعد** (`bookingBufferMinutes`): التقريب يعطيه صدفةً، وخدمةٌ مدّتها مضاعفٌ تامّ للفترة تُلصق بالتالية.

### المنتجات والمخزون
- المنتجات تُباع **مع الزيارة**: أسعارها من الكتالوج وتُضاف فوق مبلغ الخدمات المُدخل. **لا تُقبل الأسعار من العميل** — تُقرأ من قاعدة البيانات في `resolveProductLines`.
- خصم المخزون يتم **داخل معاملة الزيارة نفسها**: إما تُحفظ الزيارة ويُخصم المخزون معًا أو لا شيء. إلغاء الزيارة يعيد الكمية بحركة `RETURN` مسجّلة.
- **كل تغيير في الكمية يمر عبر `recordStockMovement`** ويُخزَّن مع `balanceAfter` — المخزون قابل للتدقيق لا رقم يُكتب فوقه. `updateProduct` لا يعدّل الكمية إطلاقًا.
- المنتج يدخل وعاء العمولة كسطر مثل الخدمة، بنسبته الخاصة إن وُجدت.

### الحضور والتنبيهات
- `AttendanceRecord` **مستقل عن جلسة الصندوق** عمدًا: الجلسة أداة مالية (متى فُتح الدرج) والحضور أداة إدارية (متى داوم). تسجيل حضور مكرر يعيد السجل القائم ولا يفتح ثانيًا.
- `lib/alerts/smart-alerts.ts` تحليلية (نفاد مخزون، انخفاض أداء حلاق، فروقات صندوق متكررة، عملاء منقطعون) وتكمّل `operation-alerts.ts` اللحظية. **كل تنبيه له رابط إجراء** — التنبيه بلا إجراء ضجيج يُدرَّب المدير على تجاهله. عتبات مضبوطة لتقليل الإزعاج: انخفاض >30% مع دخل سابق ≥200 ريال، وفروقات ≥3 جلسات.

### الفوترة (تحصيل الاشتراكات)
- **لا بوابة دفع حاليًا.** التحصيل يدوي (تحويل بنكي/نقدًا) ويسجّله **مدير المنصّة فقط** من `/platform/organizations/[id]` — الفوترة اختصاص مشغّل المنصّة لا المستأجر.
- `BillingInvoice` مصدر الحقيقة لما دُفع؛ حالة الاشتراك على `Organization` مشتقّة منه. تسجيل دفعة = فاتورة `PAID` + تمديد `currentPeriodEnd` + `subscriptionStatus: ACTIVE` + مسح `trialEndsAt`، كلها في معاملة واحدة.
- **قاعدة التمديد** (`computeNextPeriod`): التجديد قبل الانتهاء يمدّد **من تاريخ الانتهاء** لا من اليوم فلا يخسر المشترك أيامًا دفع مقابلها؛ التجديد بعد الانقطاع يبدأ من اليوم.
- `reference` (رقم الحوالة) فريد — يمنع تسجيل الدفعة مرتين. إلغاء دفعة خاطئة يعيد حساب `currentPeriodEnd` **من آخر فاتورة مدفوعة متبقية** لا بطرح المدة حسابيًا.
- **لإضافة بوابة لاحقًا:** قيمة جديدة في `PaymentProvider` + محوّل يكتب نفس `BillingInvoice` ويستدعي منطق التمديد نفسه. لا تغيير في المخطط ولا في حارس الاشتراك. وتذكّر إعفاء مسار الـ webhook من فحص Origin في `middleware.ts`.

### الاشتراك وحدود الباقة
- `assertSubscriptionActive` من `lib/plans/subscription-guard.ts` تُستدعى قبل **فتح جلسة الصندوق وتأكيد الزيارة**. القاعدة: انتهاء الاشتراك يوقف التشغيل ولا يقطع الدخول للوحة — يبقى المالك قادرًا على الترقية وقراءة تقاريره.
- `PAST_DUE` تحذير لا منع. انتهاء التجربة/الفترة أو `CANCELED` أو `SUSPENDED` = منع (402).
- حدود الباقة مفروضة على: الفروع (`createSalon`)، الحلاقين (`POST /api/dashboard/barbers`)، العملاء (`createCustomerWithLoyalty`).

### التزامن المالي
- `confirmVisit` و عمليات جلسة الصندوق/الإغلاق تستخدم معاملات `Serializable` مع إعادة محاولة عند تعارض `P2034`.
- كل زيارة تحمل `idempotencyKey` فريدًا لكل حلاق (`@@unique([barberId, idempotencyKey])`) — الضغط المزدوج لا يكرّر الزيارة.

### قواعد الأعمال الجوهرية
- **جلسة الصندوق هي القفل التشغيلي**، وليس تاريخ اليوم. لا تسجيل زيارة بدون جلسة صندوق مفتوحة (`assertOpenCashSession`). `DailyClose` تقرير فقط.
- **خصم واحد لكل زيارة**: لا يُجمع reward نقاط + حملة + مكافأة مدير معًا. الخصم لا يتجاوز المبلغ.
- **واتساب يدوي بالكامل**: النظام يجهّز روابط `wa.me` فقط؛ لا API ولا إرسال تلقائي ولا bulk.
- **التدقيق إلزامي**: كل عملية حساسة تُسجَّل عبر `writeAuditLog` (actor/before/after/IP).

## الأمان والتشغيل

- **Rate limiting** مخزّن في DB (`LoginAttempt`) مع قفل 15 دقيقة بعد 8 محاولات فاشلة خلال 5 دقائق — يصمد عبر عدة instances (`lib/auth/rate-limit.ts`).
- **الصيانة الدورية** عبر `lib/maintenance/cleanup.ts`: حذف الجلسات المنتهية/الملغاة، عدادات المحاولات المنقضية، وسجلات التدقيق/واتساب الأقدم من مدة الاحتفاظ. تُشغَّل من cron (`npm run maintenance:cleanup`) أو `POST /api/maintenance/cleanup` (مدير أو `x-maintenance-token`).
- **Logging** منظّم JSON عبر `lib/logger.ts` (`LOG_LEVEL`).
- متغيرات بيئة جديدة: `ALLOWED_ORIGINS`, `MAINTENANCE_TOKEN`, `LOG_LEVEL` (انظر `.env.example`).

## التحقق قبل أي PR

```bash
npm run typecheck && npm test && npm run lint && npm run build
```

CI على GitHub Actions (`.github/workflows/ci.yml`) يشغّلها مع خدمة PostgreSQL على كل push/PR إلى `main`.

## إضافة migration

عدّل `prisma/schema.prisma` ثم `npm run prisma:migrate -- --name <اسم>`. تحقق من الترتيب على قاعدة فارغة (انظر قسم "فحص قاعدة جديدة من الصفر" في README).

> **افحص ملف الهجرة المولَّد قبل اعتماده — إلزامي.** طبقة عزل المستأجرين في قاعدة
> البيانات (قيود `%_tenant_fkey` والفهارس المركّبة `*_id_organizationId_key`
> ومحفّزات `VisitService/VisitProduct`) مكتوبة SQL يدويًا في
> `20260811010000_security_hardening` ولا يمثّلها مخطط Prisma. لذلك يعتبرها
> `prisma migrate dev` **انحرافًا** ويولّد لها `DROP CONSTRAINT` / `DROP INDEX`
> في هجرتك الجديدة. احذف تلك الأسطر يدويًا وأبقِ إضافاتك فقط، وأضف لأي جدول
> جديد يحمل `organizationId` قيوده الخاصة على النمط نفسه. اختبار
> `tests/security-hardening-phase2.test.ts` هو شبكة الأمان الأخيرة.
>
> **وتحقق من ترتيب اسم الهجرة:** الطابع الزمني المولَّد من ساعة الجهاز قد يسبق
> هجرات موجودة، فتُطبَّق قبل ما تعتمد عليه على قاعدة جديدة. أعِد تسميتها لتلي
> أحدث هجرة، ثم تأكّد بـ `prisma migrate deploy` على قاعدة فارغة.
